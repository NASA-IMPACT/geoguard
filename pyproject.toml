[project]
name = "geoguard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=1.93.0",
    "pydantic-settings>=2.14.1",
]

[dependency-groups]
dev = [
    "marimo>=0.23.5",
    "pre-commit>=4.0.1",
]
